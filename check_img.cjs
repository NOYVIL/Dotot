const fs = require('fs');
const grids = ['grid1.png', 'grid2.png', 'grid3.png', 'grid4.png', 'grid5.png', 'grid6.png'];
for (const g of grids) {
  // read a few bytes to get png dimensions if possible, or just use a library like 'image-size' if available.
}
